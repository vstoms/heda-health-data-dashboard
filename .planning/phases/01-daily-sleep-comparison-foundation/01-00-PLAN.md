---
phase: 01-daily-sleep-comparison-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/test/setup.ts
  - src/services/parsers/sleepParser.test.ts
  - src/services/metrics/sleepDailyComparison.test.ts
  - src/components/charts/sleep/SleepComparisonPhase1.test.tsx
autonomous: true
requirements:
  - SLPD-01
  - SLPD-02
  - SLPD-03
  - SLPD-04
  - SLPG-01
user_setup: []
must_haves:
  truths:
    - "Phase 1 implementation tasks have fast automated checks that validate parser, aggregation, and chart contracts before feature changes proceed."
    - "The validation loop can catch regressions in wake-day grouping, effective-duration semantics, and missing-need presentation during execution."
  artifacts:
    - path: "vitest.config.ts"
      provides: "Project test runner configuration for TS/React code"
      min_lines: 20
    - path: "src/services/parsers/sleepParser.test.ts"
      provides: "Golden-rule parser regression fixtures for wake-day and duration semantics"
      min_lines: 40
    - path: "src/services/metrics/sleepDailyComparison.test.ts"
      provides: "Daily comparison contract tests for missing-need and aggregation behavior"
      min_lines: 40
    - path: "src/components/charts/sleep/SleepComparisonPhase1.test.tsx"
      provides: "UI-level Phase 1 missing-need rendering baseline test"
      min_lines: 40
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "Vitest setupFiles registration"
      pattern: "setupFiles"
    - from: "src/components/charts/sleep/SleepComparisonPhase1.test.tsx"
      to: "src/components/charts/sleep/SleepDurationChart.tsx"
      via: "Component test target linkage"
      pattern: "SleepDurationChart"
---

<objective>
Establish Wave 0 validation infrastructure required by Nyquist before Phase 1 implementation.

Purpose: Ensure implementation tasks can run fast, repeatable automated checks.
Output: Vitest setup plus parser/metrics/chart baseline test files referenced by VALIDATION.md.
</objective>

<execution_context>
@/home/viggo/.codex/get-shit-done/workflows/execute-plan.md
@/home/viggo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-VALIDATION.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Vitest test infrastructure</name>
  <files>package.json, vitest.config.ts, src/test/setup.ts</files>
  <action>Add Vitest + jsdom + testing-library dependencies, add test scripts, and create Vitest configuration with setup file support for this React/TS repo. Keep configuration minimal and aligned with existing Vite alias behavior.</action>
  <verify>npx vitest run --reporter=dot --passWithNoTests</verify>
  <done>Vitest commands run successfully and can discover tests in src.</done>
</task>

<task type="auto">
  <name>Task 2: Create baseline parser and metrics contract tests</name>
  <files>src/services/parsers/sleepParser.test.ts, src/services/metrics/sleepDailyComparison.test.ts</files>
  <action>Create baseline tests and fixtures covering wake-day attribution, awake exclusion, fallback duration from phases, and missing sleep-need semantics. Use deterministic fixture data and clear assertion names so later plan tasks can extend them safely.</action>
  <verify>npx vitest run src/services/parsers/sleepParser.test.ts src/services/metrics/sleepDailyComparison.test.ts</verify>
  <done>Baseline parser and metrics test files exist and pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add baseline Sleep chart rendering test harness</name>
  <files>src/components/charts/sleep/SleepComparisonPhase1.test.tsx</files>
  <action>Create initial component test harness for Phase 1 sleep comparison rendering and missing-need states. Keep assertions focused on presence/visibility semantics rather than Phase 2 signed-gap visuals.</action>
  <verify>npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx</verify>
  <done>Chart baseline test file exists and passes with current chart behavior or clearly documented TODO assertions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run --reporter=dot --passWithNoTests
- [ ] npx vitest run src/services/parsers/sleepParser.test.ts src/services/metrics/sleepDailyComparison.test.ts
- [ ] npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Wave 0 prerequisites from VALIDATION.md are satisfied for Phase 1 plans
  </success_criteria>

<output>
After completion, create `.planning/phases/01-daily-sleep-comparison-foundation/01-00-SUMMARY.md`
</output>
