---
phase: 01-daily-sleep-comparison-foundation
plan: 01
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - src/types/index.ts
  - src/services/parsers/sleepParser.ts
  - src/services/parsers/sleepParser.test.ts
  - src/services/metrics/sleepDailyComparison.ts
  - src/services/metrics/index.ts
  - src/services/metrics/sleepDailyComparison.test.ts
autonomous: true
requirements:
  - SLPD-01
  - SLPD-02
  - SLPD-03
  - SLPD-04
user_setup: []
must_haves:
  truths:
    - "Daily aggregation groups sleep sessions by wake day (session end date)."
    - "Effective sleep duration excludes awake time in daily comparison data."
    - "When source sleep duration is missing or zero, duration is derived from light+deep+REM phases."
    - "Days without sleep-need are represented explicitly as missing, never coerced to zero."
  artifacts:
    - path: "src/services/metrics/sleepDailyComparison.ts"
      provides: "Canonical daily sleep comparison dataset and aggregation policy"
      min_lines: 60
    - path: "src/services/parsers/sleepParser.ts"
      provides: "Sleep-need parsing with stable missing-state semantics"
      contains: "sleepNeed"
    - path: "src/services/metrics/sleepDailyComparison.test.ts"
      provides: "Regression tests for wake-day attribution and missing-need behavior"
      min_lines: 80
  key_links:
    - from: "src/services/parsers/sleepParser.ts"
      to: "src/types/index.ts"
      via: "SleepData shape including sleep-need field"
      pattern: "sleepNeed"
    - from: "src/services/metrics/sleepDailyComparison.ts"
      to: "src/services/parsers/sleepParser.ts"
      via: "Consumes parsed sleep records and preserves parser rules"
      pattern: "isNap|duration|awake|end"
    - from: "src/services/metrics/sleepDailyComparison.test.ts"
      to: "src/services/metrics/sleepDailyComparison.ts"
      via: "Validates aggregation contracts"
      pattern: "buildDailySleepComparison|aggregate"
---

<objective>
Build the canonical Phase 1 sleep comparison data layer.

Purpose: Ensure downstream charting uses trustworthy, testable daily sleep values.
Output: Parser/type updates plus a metrics service that enforces wake-day, duration, and missing-need rules.
</objective>

<execution_context>
@/home/viggo/.codex/get-shit-done/workflows/execute-plan.md
@/home/viggo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-CONTEXT.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-RESEARCH.md

@src/services/parsers/sleepParser.ts
@src/services/metrics/sleepStatsCalculator.ts
@src/services/metrics/index.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend sleep data contract and parser sleep-need extraction</name>
  <files>src/types/index.ts, src/services/parsers/sleepParser.ts, src/services/parsers/sleepParser.test.ts</files>
  <action>Add explicit sleep-need field(s) to the sleep data contract and parse Withings sleep-need source columns in sleepParser. Keep current golden rules unchanged: wake-day attribution by end date, effective duration excludes awake, and phase-based fallback when duration is missing/zero. Missing sleep-need must remain null/undefined state (never 0 coercion). Update parser tests with assertions for sleep-need extraction when present, absent-column handling, and null-preserving missing-state semantics.</action>
  <verify>npx vitest run src/services/parsers/sleepParser.test.ts</verify>
  <done>Sleep records include parsed sleep-need when present and explicit missing state otherwise, with no regression in existing parser behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical daily sleep comparison aggregation service</name>
  <files>src/services/metrics/sleepDailyComparison.ts, src/services/metrics/index.ts</files>
  <action>Create a dedicated metrics module that produces day-level comparison points with fields for date, effective duration, sleep-need value/missing marker, and non-computed gap placeholder for missing-need days. Aggregate by wake-day, sum non-nap sessions on the same day, and respect nap inclusion toggle input. Reuse existing overlap semantics rather than introducing a new policy.</action>
  <verify>npx vitest run src/services/metrics/sleepDailyComparison.test.ts</verify>
  <done>Service API returns stable daily comparison points satisfying SLPD-01..04 semantics and is exported for dashboard consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for Phase 1 data correctness contracts</name>
  <files>src/services/metrics/sleepDailyComparison.test.ts</files>
  <action>Create focused tests for wake-day grouping across midnight, awake-time exclusion in effective duration, fallback duration from sleep phases, and explicit missing-need handling (no zero coercion). Include same-day multi-session aggregation behavior and nap toggle behavior.</action>
  <verify>npx vitest run src/services/metrics/sleepDailyComparison.test.ts --passWithNoTests</verify>
  <done>Regression tests cover each SLPD requirement and pass locally.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/services/metrics/sleepDailyComparison.test.ts --passWithNoTests
- [ ] npx vitest run src/services/parsers/sleepParser.test.ts
- [ ] npm run build
- [ ] Daily comparison points preserve missing-need state and wake-day grouping in test fixtures
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Canonical daily comparison service is available for dashboard integration in later plan(s)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-daily-sleep-comparison-foundation/01-01-SUMMARY.md`
</output>
