---
phase: 01-daily-sleep-comparison-foundation
plan: 02
type: execute
wave: 3
depends_on:
  - "01-01"
files_modified:
  - src/components/dashboard/hooks/useDashboardMetrics.ts
  - src/components/charts/sleep/SleepChart.tsx
  - src/components/charts/sleep/SleepDurationChart.tsx
  - src/components/charts/sleep/SleepComparisonPhase1.test.tsx
  - src/i18n/en.json
  - src/i18n/fr.json
autonomous: true
requirements:
  - SLPG-01
  - SLPD-04
user_setup: []
must_haves:
  truths:
    - "Sleep section shows a daily chart series for sleep duration and sleep need values."
    - "Days with missing sleep-need remain visible and are explicitly marked as need unavailable."
    - "Users can inspect a day and see duration plus explicit unavailable-need messaging when need is missing."
  artifacts:
    - path: "src/components/dashboard/hooks/useDashboardMetrics.ts"
      provides: "Wiring from canonical daily comparison service into dashboard view model"
      min_lines: 20
    - path: "src/components/charts/sleep/SleepChart.tsx"
      provides: "Sleep section integration of daily duration-vs-need chart data"
      min_lines: 20
    - path: "src/i18n/en.json"
      provides: "User-facing copy for missing sleep-need indicators and labels"
      contains: "sleepNeed"
  key_links:
    - from: "src/components/dashboard/hooks/useDashboardMetrics.ts"
      to: "src/services/metrics/sleepDailyComparison.ts"
      via: "Hook-level series construction from canonical service output"
      pattern: "sleepDailyComparison|daily"
    - from: "src/components/charts/sleep/SleepChart.tsx"
      to: "src/components/charts/sleep/SleepDurationChart.tsx"
      via: "Passes duration-vs-need series and detail payload"
      pattern: "duration|need"
    - from: "src/components/charts/sleep/SleepDurationChart.tsx"
      to: "src/i18n/en.json"
      via: "Localized labels/tooltips for duration and need display"
      pattern: "t\\("
---

<objective>
Integrate Phase 1 daily comparison data into the Sleep section charting pipeline.

Purpose: Deliver the Phase 1 user-visible daily duration-versus-need chart baseline with explicit missing-need behavior.
Output: Dashboard hook wiring + sleep chart integration + i18n labels needed for Phase 1 behavior.
</objective>

<execution_context>
@/home/viggo/.codex/get-shit-done/workflows/execute-plan.md
@/home/viggo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-CONTEXT.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-RESEARCH.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-01-SUMMARY.md

@src/components/dashboard/hooks/useDashboardMetrics.ts
@src/components/charts/sleep/SleepChart.tsx
@src/components/charts/sleep/SleepDurationChart.tsx
@src/i18n/en.json
@src/i18n/fr.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire daily comparison dataset into dashboard sleep metrics</name>
  <files>src/components/dashboard/hooks/useDashboardMetrics.ts</files>
  <action>Consume the canonical daily comparison service from Plan 01 and expose sleep duration and sleep-need day-level series to sleep chart components. Ensure missing-need days are represented without fabricating need/gap values. Keep existing filters and nap-toggle behavior aligned with prior dashboard semantics. Update summary aggregation outputs so missing-need days are counted separately and excluded from need/gap aggregates per locked context decisions.</action>
  <verify>npx vitest run src/services/metrics/sleepDailyComparison.test.ts</verify>
  <done>Dashboard metrics hook provides a stable data contract for duration-vs-need rendering, and summary outputs include missing-need day counts while excluding those days from need/gap aggregates.</done>
</task>

<task type="auto">
  <name>Task 2: Render duration-vs-need baseline chart in Sleep section</name>
  <files>src/components/charts/sleep/SleepChart.tsx, src/components/charts/sleep/SleepDurationChart.tsx, src/components/charts/sleep/SleepComparisonPhase1.test.tsx</files>
  <action>Update Sleep chart composition to render daily duration and daily sleep-need values in the Sleep section for Phase 1. Keep missing-need days visible, show explicit unavailable indicator in tooltip/day detail, and avoid introducing signed-gap color semantics (reserved for Phase 2). Convert the Wave 0 chart test harness into concrete assertions for duration-vs-need rendering, visibility of missing-need days, and explicit unavailable messaging.</action>
  <verify>npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx</verify>
  <done>Sleep section visibly shows duration and need daily series with explicit missing-need treatment and no regressions in existing sleep charts.</done>
</task>

<task type="auto">
  <name>Task 3: Add internationalized copy for Phase 1 need-missing states</name>
  <files>src/i18n/en.json, src/i18n/fr.json</files>
  <action>Add translation keys for sleep-need labels, missing-need indicator text, and related tooltip/day-detail copy used by Phase 1 chart behavior. Keep key naming consistent with existing charts.sleep namespaces and avoid hardcoded strings in components.</action>
  <verify>npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx --passWithNoTests</verify>
  <done>All new user-facing sleep-need text renders through i18n keys in both English and French resources.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/services/metrics/sleepDailyComparison.test.ts
- [ ] npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx
- [ ] npm run build
- [ ] Sleep section displays duration + need daily series for filtered date range
- [ ] Missing-need days are counted separately and excluded from need/gap summary aggregates
- [ ] Missing-need day shows explicit unavailable messaging and remains visible in chart/detail
- [ ] No new hardcoded strings introduced in updated components
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 chart baseline is ready for Phase 2 signed-gap visualization work
  </success_criteria>

<output>
After completion, create `.planning/phases/01-daily-sleep-comparison-foundation/01-02-SUMMARY.md`
</output>
