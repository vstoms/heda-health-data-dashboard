---
phase: 02-gap-insight-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/hooks/useDashboardMetrics.ts
  - src/components/charts/sleep/SleepDurationChart.tsx
  - src/components/charts/sleep/SleepChart.tsx
  - src/components/charts/sleep/SleepComparisonPhase1.test.tsx
autonomous: true
requirements:
  - SLPG-02
user_setup: []
must_haves:
  truths:
    - "User can view a signed daily gap where each day represents duration minus sleep need."
    - "User can distinguish deficit and surplus days at a glance from chart polarity and visual encoding."
  artifacts:
    - path: "src/components/charts/sleep/SleepDurationChart.tsx"
      provides: "Signed gap visualization with zero baseline and deficit/surplus semantics"
      min_lines: 60
    - path: "src/components/dashboard/hooks/useDashboardMetrics.ts"
      provides: "Gap summary metrics and day-level gap payloads for chart consumption"
      min_lines: 20
    - path: "src/components/charts/sleep/SleepComparisonPhase1.test.tsx"
      provides: "Assertions for signed gap rendering and deficit/surplus distinction"
      min_lines: 40
  key_links:
    - from: "src/components/dashboard/hooks/useDashboardMetrics.ts"
      to: "src/components/charts/sleep/SleepDurationChart.tsx"
      via: "Passes signed gap and summary data into chart component props"
      pattern: "gapSeconds|comparisonSummary"
    - from: "src/components/charts/sleep/SleepDurationChart.tsx"
      to: "src/components/charts/sleep/SleepComparisonPhase1.test.tsx"
      via: "Chart behavior covered by signed-gap assertions"
      pattern: "gap|deficit|surplus|zero"
---

<objective>
Implement signed daily gap visualization semantics in the Sleep section.

Purpose: Deliver the core Phase 2 behavior where users can interpret deficit/surplus without manual calculation.
Output: Signed gap chart behavior, zero-baseline visual logic, and regression tests.
</objective>

<execution_context>
@/home/viggo/.codex/get-shit-done/workflows/execute-plan.md
@/home/viggo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gap-insight-visualization/02-RESEARCH.md
@.planning/phases/01-daily-sleep-comparison-foundation/01-02-SUMMARY.md

@src/components/dashboard/hooks/useDashboardMetrics.ts
@src/components/charts/sleep/SleepChart.tsx
@src/components/charts/sleep/SleepDurationChart.tsx
@src/components/charts/sleep/SleepComparisonPhase1.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute signed gap-focused chart summary and series metadata</name>
  <files>src/components/dashboard/hooks/useDashboardMetrics.ts</files>
  <action>Extend sleep comparison summary outputs to expose signed gap semantics explicitly for Phase 2 (deficit, surplus, neutral counts and chart-facing range hints). Keep missing-need days excluded from aggregate gap calculations per Phase 1 context lock.</action>
  <verify>npx vitest run src/services/metrics/sleepDailyComparison.test.ts</verify>
  <done>Hook exports signed-gap summary metadata used by chart rendering logic, with missing-need aggregation behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Render signed gap with zero baseline and deficit/surplus visual polarity</name>
  <files>src/components/charts/sleep/SleepDurationChart.tsx, src/components/charts/sleep/SleepChart.tsx</files>
  <action>Add signed-gap visualization path to Sleep duration-vs-need charting: each day gap is derived from existing daily comparison points, zero baseline is visible, and deficit vs surplus are immediately distinguishable through consistent visual encoding. Keep need-missing days visible but non-computed for gap.</action>
  <verify>npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx</verify>
  <done>Chart clearly shows signed gap behavior with stable handling for missing-need days.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen chart tests for signed-gap readability guarantees</name>
  <files>src/components/charts/sleep/SleepComparisonPhase1.test.tsx</files>
  <action>Update/extend tests to assert signed gap values, deficit/surplus distinction, and presence of zero-baseline semantics in chart configuration/output behaviors. Ensure assertions are concrete and not TODO placeholders.</action>
  <verify>npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx</verify>
  <done>Regression tests fail on incorrect signed-gap polarity behavior and pass for intended visualization semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx vitest run src/services/metrics/sleepDailyComparison.test.ts
- [ ] npx vitest run src/components/charts/sleep/SleepComparisonPhase1.test.tsx
- [ ] npm run build
- [ ] Signed-gap chart behavior distinguishes deficit and surplus without manual calculation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SLPG-02 is satisfied with user-visible signed-gap behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/02-gap-insight-visualization/02-01-SUMMARY.md`
</output>
